# NEVER commit credentials — this is a PUBLIC repo
config/config.properties
*.env
access_token*
request_token*

# Local data
*.db
*.db-journal
# root-level data dir only — a bare "data/" would also ignore
# src/main/java/**/data/ (it silently untracked the data package!)
/data/

# Generated backtest reports
/reports/

# Build
target/
*.class
*.jar

# IDE
.idea/
*.iml
.vscode/
.classpath
.project
.settings/

# OS
.DS_Store
Thumbs.db

# Staging area for files Claude's session moves aside for manual deletion
# (its shell can move files here but cannot delete them)
_to_delete/
